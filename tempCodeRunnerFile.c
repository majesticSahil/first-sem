char st[]="sahil";
    // printf("%d\n",strlen(st));
    // char target[30];
    // //strcpy() will copy the content of 2nd string to 1st string
    // strcpy(target,st);
    // puts(target);
    // //strcat() is used to concatenate string
    // strcat(st,target);
    // puts(st);